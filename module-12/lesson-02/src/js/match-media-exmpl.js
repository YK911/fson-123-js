// window.addEventListener('resize', () => {
//   let mql = window.matchMedia('(width <= 600px)');
//   console.log('🚀 ~ mql:', mql);
// });

export const perPage = {
  sm: 6,
  md: 8,
  lg: 12,
};

export const media = {
  sm: '(width <= 600px)',
  md: '(width >= 768px)',
  lg: '(width >= 1200px)',
};

// perPage[sm];
